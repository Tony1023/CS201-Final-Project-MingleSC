<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="suggestion.suggestionMapper">
  <select id="computeSuggestions" parameterType="map" resultType="java.lang.Integer">

SELECT user_id FROM user WHERE NOT user_id=#{this_id};
UPDATE user SET user_id=1 WHERE user_id=1;

  </select>
</mapper>